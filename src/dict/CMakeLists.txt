add_library(
    dictionary_db
    dictionary.cpp
    databasemanager.cpp
    yomidbbuilder.c
    expression.h
)
target_link_libraries(
    dictionary_db
    ${SQLITE_LIBRARY}
    ${JSONC}
    ${LIBZIP}
    ${MECAB_LIBRARY}
    Qt5::Core
    Qt5::Widgets
)