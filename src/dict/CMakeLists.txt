add_library(
    dictionary_db
    dictionary.cpp
    databasemanager.cpp
    yomidbbuilder.c
    expression.h
)
target_link_libraries(
    dictionary_db
    ${SQLITE}
    ${JSONC}
    ${LIBZIP}
    ${MECAB}
    Qt::Core
)